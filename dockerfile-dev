FROM python:3.11.3

WORKDIR /app
COPY requirements.txt ./
RUN ["python", "-m", "pip", "install", "--upgrade", "pip"]
RUN ["pip", "install", "-r", "requirements.txt"]

EXPOSE 80

COPY . .
CMD ["gunicorn", "--bind", "0.0.0.0:80", "release_backend.wsgi:application"]
